# Dependencies
node_modules/

# Local environment (never commit secrets / machine-specific paths)
.env
.env.local

# Generated knowledge base (re-generate with `node crawl.js`)
docs/

# Downloaded PDF binaries (large; re-crawl with `node crawl-pdfs.js`)
pdfs/

# Generated reports
sync-report.json
sync-pdfs-report.json

# Manifest (re-generate with `node build-manifest.js`)
manifest.json

# Local editor / OS cruft
.DS_Store
*.swp
